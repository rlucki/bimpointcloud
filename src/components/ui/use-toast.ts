
import { useToast, toast } from "@/hooks/use-toast";

// Añadir aquí un log para verificar que se está usando correctamente
console.log("[use-toast] Re-exporting toast hooks");

export { useToast, toast };
